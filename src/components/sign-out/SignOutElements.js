import styled from 'styled-components'

export const SignOutBtnElement = styled.button`
    background: none;
    color: var(--primary);
    font-size: 1.1rem;

    padding: 6px;
    border-radius: 4px;
    border: none;
	cursor: pointer;

    :hover{
        color: coral;
    }
`