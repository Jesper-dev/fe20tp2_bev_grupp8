import styled from 'styled-components'

export const SignOutBtnElement = styled.button`
    background: none;
    color: var(--primary);

    padding: 4px;
    border-radius: 4px;
    border: none;
	cursor: pointer;

    :hover{
        color: coral;
    }
`